const IconGeneric = ({ name }: { name: string }) => (
  <div className="w-12 rounded-full bg-primary text-neutral-focus">
    <span className="text-xl">{name}</span>
  </div>
)

export default IconGeneric
